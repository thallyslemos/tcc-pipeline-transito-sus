"use client";

import { useEffect, useState } from "react";
import { fetchSimMetadata, fetchSimMunicipios } from "@/lib/api";
import { formatNumber } from "@/lib/format";
import type { SimCatalog, SimMunicipio } from "@/lib/types";

export default function DadosPage() {
  const [catalog, setCatalog] = useState<SimCatalog | null>(null);
  const [rows, setRows] = useState<SimMunicipio[]>([]);
  const [search, setSearch] = useState("");
  const [page, setPage] = useState(1);
  const [total, setTotal] = useState(0);
  const [dimensao, setDimensao] = useState<"ocorrencia" | "residencia">("ocorrencia");
  useEffect(() => { fetchSimMetadata().then(setCatalog); }, []);
  useEffect(() => { fetchSimMunicipios({ dimensao, municipio: search }, page, 25).then((result) => { setRows(result.municipios); setTotal(result.total); }); }, [dimensao, page, search]);
  const pages = Math.max(1, Math.ceil(total / 25));
  return <div className="space-y-5"><div><h1 className="text-lg font-bold" style={{ color: "var(--fg)" }}>Dados e metadados</h1><p className="text-xs" style={{ color: "var(--fg-muted)" }}>Fontes, cobertura, grao, hashes e consulta paginada dos marts SIM.</p></div><div className="grid gap-3 md:grid-cols-2">{catalog?.datasets.map((dataset) => <article key={dataset.id} className="rounded-xl p-4" style={{ backgroundColor: "var(--bg-card)", border: "1px solid var(--border)" }}><div className="flex items-start justify-between gap-3"><h2 className="text-sm font-semibold" style={{ color: "var(--fg)" }}>{dataset.id}</h2><span className="rounded-full px-2 py-0.5 text-[10px]" style={{ backgroundColor: dataset.status === "validated" ? "var(--success-soft)" : "var(--warning-soft)", color: "var(--fg-secondary)" }}>{dataset.status}</span></div><p className="mt-2 text-xs" style={{ color: "var(--fg-secondary)" }}>{dataset.provider} - {dataset.grain ?? "sem grao informado"}</p><p className="mt-1 text-[11px]" style={{ color: "var(--fg-muted)" }}>Linhas: {dataset.quality?.rows == null ? "N/D" : formatNumber(Number(dataset.quality.rows))} - {dataset.sha256 ? `SHA-256 ${dataset.sha256.slice(0, 12)}...` : "hash nao informado"}</p></article>)}</div><section className="rounded-xl p-4" style={{ backgroundColor: "var(--bg-card)", border: "1px solid var(--border)" }}><div className="flex flex-wrap gap-2"><select aria-label="Dimensao" value={dimensao} onChange={(event) => { setDimensao(event.target.value as "ocorrencia" | "residencia"); setPage(1); }} className="rounded-lg px-3 py-2 text-sm" style={{ backgroundColor: "var(--bg-card)", color: "var(--fg)", border: "1px solid var(--border)" }}><option value="ocorrencia">Ocorrencia</option><option value="residencia">Residencia</option></select><input value={search} onChange={(event) => { setSearch(event.target.value); setPage(1); }} placeholder="Buscar municipio ou codigo" className="min-w-0 flex-1 rounded-lg px-3 py-2 text-sm" style={{ backgroundColor: "var(--bg)", color: "var(--fg)", border: "1px solid var(--border)" }} /></div><div className="mt-4 overflow-x-auto"><table className="w-full text-sm"><thead style={{ backgroundColor: "var(--bg-muted)" }}><tr className="text-xs" style={{ color: "var(--fg-muted)" }}><th className="px-3 py-2 text-left">Codigo</th><th className="px-3 py-2 text-left">Municipio</th><th className="px-3 py-2 text-left">UF</th><th className="px-3 py-2 text-right">Obitos</th><th className="px-3 py-2 text-right">Taxa / 100 mil</th></tr></thead><tbody>{rows.map((row) => <tr key={row.cod_mun_ibge} style={{ borderBottom: "1px solid var(--border)" }}><td className="px-3 py-2 font-mono text-xs">{row.cod_mun_ibge}</td><td className="px-3 py-2">{row.municipio}</td><td className="px-3 py-2">{row.uf}</td><td className="px-3 py-2 text-right">{formatNumber(row.obitos)}</td><td className="px-3 py-2 text-right">{row.taxa_obitos_100mil == null ? "N/D" : row.taxa_obitos_100mil.toFixed(1)}</td></tr>)}</tbody></table></div><div className="mt-3 flex items-center justify-between text-xs" style={{ color: "var(--fg-muted)" }}><span>{total ? `${(page - 1) * 25 + 1}?${Math.min(page * 25, total)} de ${total}` : "Sem dados"}</span><div className="flex gap-2"><button disabled={page <= 1} onClick={() => setPage((value) => Math.max(1, value - 1))} className="rounded px-2 py-1 disabled:opacity-30" style={{ border: "1px solid var(--border)" }}>Anterior</button><span>{page} / {pages}</span><button disabled={page >= pages} onClick={() => setPage((value) => Math.min(pages, value + 1))} className="rounded px-2 py-1 disabled:opacity-30" style={{ border: "1px solid var(--border)" }}>Proxima</button></div></div></section></div>;
}
